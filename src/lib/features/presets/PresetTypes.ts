export type OptionValue = string | number | null | boolean;

export type OptionsProp = {
  filter: OptionValue;
  font: OptionValue;
  fontSize: OptionValue;
  fontCapitalization: boolean;
  fontPosition: OptionValue;
  aspectRatio: OptionValue;
  backgroundColor: string;
  fontColor: string;
  scaling: number;
  brollToggle: boolean;
  glowColor: string;
  shadowWidth: OptionValue;
  strokeColor: string;
  strokeWidth: OptionValue;
};
